DATABASE_URL=
JWT_SECRET_KEY=
JWT_ALGORITHM=
JWT_EXPIRE_MINUTES=
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL_NAME=
# 思考模式：true 时启用模型推理（extra_body thinking + reasoning_effort），思维链经
# reasoning_content 透传给前端展示；false 回退普通对话。LLM_REASONING_EFFORT：high/medium/low。
LLM_THINKING_ENABLED=
LLM_REASONING_EFFORT=

# MySQL MCP：用 venv 内 python 拉起 mysql-mcp-server，连接走只读账号 mcp_ro（仅 SELECT）
MYSQL_MCP_ENABLED=
MYSQL_MCP_COMMAND=
MYSQL_MCP_ARGS=
MYSQL_MCP_DATABASE_URL=


MEMORY_MCP_ENABLED=
MEMORY_MCP_COMMAND=
MEMORY_MCP_ARGS=
MEMORY_MCP_FILE_PATH=

# 阿里云 OSS
OSS_ACCESS_KEY_ID=
OSS_ACCESS_KEY_SECRET=
OSS_ENDPOINT=
OSS_BUCKET=
OSS_PUBLIC_BASE_URL=

# 文本向量化（Qwen3-Embedding via DashScope 兼容模式）
EMBEDDING_API_KEY=
EMBEDDING_BASE_URL=
EMBEDDING_MODEL_NAME=
EMBEDDING_BATCH_SIZE=

# Milvus 向量库（该实例 19530 为明文 gRPC，用 http:// 而非 https://）
# MILVUS_URI=http://127.0.0.1:19530
MILVUS_URI=
MILVUS_TOKEN=
MILVUS_COLLECTION=
MILVUS_KB_COLLECTION=
MILVUS_DIM=
MILVUS_USER=
MILVUS_PASSWORD=

# 知识库文档导入：切分参数（字符数）。
KB_CHUNK_SIZE=
KB_CHUNK_OVERLAP=
# RAG 检索：最终拼进上下文 / 返回给模型的片段数。
# 启用 rerank 时，先从 Milvus 粗召回的候选数（再 rerank 取前 rag_top_k）。
# RAG 召回 + rerank 重排：先从 Milvus 召回 10 条，再 rerank 取前 RAG_TOP_K(5)
RAG_TOP_K=
RAG_RECALL_K=
RERANK_ENABLED=
RERANK_MODEL=
# RERANK_API_KEY 留空则复用 EMBEDDING_API_KEY
RERANK_BASE_URL=

PDF_VL_ENABLED=
VL_MODEL=
VL_API_KEY=
VL_BASE_URL=
VL_TIMEOUT=

# 关闭联网搜索，两个工具都不绑定给模型
WEB_SEARCH_ENABLED=
TAVILY_API_KEY=
TAVILY_BASE_URL=
WEB_SEARCH_MAX_RESULTS=
WEB_SEARCH_TIMEOUT=
WEB_SEARCH_FETCH_CONTENT=
WEB_SEARCH_FETCH_COUNT=
WEB_SEARCH_CONTENT_CHARS=


GUEST_DAILY_LIMIT=
CORS_ORIGINS=
