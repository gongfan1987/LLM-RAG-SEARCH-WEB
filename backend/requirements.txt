fastapi==0.138.0
uvicorn[standard]==0.49.0
sqlalchemy==2.0.35
pymysql==1.1.1
cryptography==43.0.1
pydantic==2.13.4
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
# httpx 0.28.1：ddgs（联网搜索）要求 >=0.28.1，故由 0.27.2 升级；rerank/embedding/vl/oss 等
# 现有 httpx 调用经全量测试验证无回归。
httpx==0.28.1
langchain-openai==1.3.2
langchain-core==1.4.8
# MCP 工具接入（要求 fastapi>=0.118 / pydantic>=2.11 / starlette>=0.49，已随上方一并升级）
langchain-mcp-adapters==0.3.0
mcp==1.28.0
# MySQL MCP server（路线 A：与后端同 venv，python -m mysql_mcp_server 拉起）
mysql-mcp-server==0.4.2
# 联网搜索工具（见 app/llm/web_search.py）：DuckDuckGo 免费搜索（无需 key）。
# Tavily 走已有的 httpx，不额外引入 SDK。
ddgs==9.14.4
# 阿里云 OSS 官方 SDK（对象存储，见 app/utils/oss.py）
oss2==2.19.1
# Milvus 官方 SDK（向量库，见 app/utils/milvus.py）
pymilvus==3.0.0
# 知识库文档解析（见 app/services/document_parsing/）：PDF / Word / Excel
pymupdf==1.27.2.3
# PDF→Markdown（表格保留为 markdown 表，供 RAG 切分），PyMuPDF 官方出品
pymupdf4llm==1.27.2.3
python-docx==1.2.0
openpyxl==3.1.5
# 图片 OCR（见 app/utils/ocr.py）；较重，未安装时自动降级为不识别图中文字
rapidocr-onnxruntime==1.4.4
